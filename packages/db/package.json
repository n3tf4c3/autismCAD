{
  "name": "@autismcad/db",
  "version": "0.1.0",
  "private": true,
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:migrate": "drizzle-kit migrate --config drizzle.config.ts",
    "db:check": "drizzle-kit check --config drizzle.config.ts",
    "db:push": "node -e \"if(process.env.DB_PUSH_SANDBOX!=='1'){console.error('db:push desabilitado (achado 105): cria schema sem as funcoes/triggers das migrations. Use db:migrate. Sandbox descartavel: DB_PUSH_SANDBOX=1 npm run db:push');process.exit(1)}\" && drizzle-kit push --config drizzle.config.ts",
    "db:studio": "drizzle-kit studio --config drizzle.config.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "dotenv": "^17.2.4",
    "drizzle-kit": "^0.31.10"
  }
}
